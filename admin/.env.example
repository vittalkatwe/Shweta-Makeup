# Copy this file to ".env.local" in the same folder.
# NEXT_PUBLIC_* vars are inlined at BUILD time (Docker build arg on Render);
# a missing value silently falls back to http://localhost:5001 (src/lib/api.ts).
NEXT_PUBLIC_API_URL=http://localhost:5001
